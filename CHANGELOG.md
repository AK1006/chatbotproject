# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]
### Added
- Standardized project structure.
- Tests for intent prediction and chat flow.
- Setup scripts and clear documentation.
- `requirements.txt` for easier dependency management.
- `.env.example` for environment variable configurations.

### Changed
- Standardized the code format in `main.py` and `trainingData.py`.
- Replaced outdated Keras optimizer arguments.

### Fixed
- Fixed bug with `model.save` call taking incorrect arguments.
